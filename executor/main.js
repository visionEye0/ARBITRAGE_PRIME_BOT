// * Before starting our contract, see if muffinswap contract which we are approving for our wallet is not malicious
// * use the packages correct versions used in fork_muffinswap repo, otherwise conflicts may occur
// * execute the contract when ethereum gas is low and there isn't much volatitlity in the market

const { ethers } = require("ethers")

// const provider = 

//Project dropped due ECDSA vulns in openzeppelin library which is depended by
//@muffinfi/muffin-sdk
//we gonna do it manually for now using searcher & maybe automate it further down the time












